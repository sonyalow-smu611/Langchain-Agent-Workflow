# Security Guidelines

Check for:

- hardcoded secrets
- unsafe user input
- SQL injection
- authentication bypasses
- authorization errors
- insecure API endpoints
- sensitive information in logs

Never expose API keys or credentials in generated source code.